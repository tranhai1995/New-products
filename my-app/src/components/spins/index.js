import React from "react";
import { CustomSpin, CustomIcon } from "@zef/components/spins/style";

const Spins = () => {
  return <CustomSpin size="large" indicator={<CustomIcon spin />} />;
};

export default Spins;
